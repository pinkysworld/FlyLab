"""Repository-level scripts (not part of the installed ``flylab`` package).

``flylab reproduce-paper`` imports :func:`scripts.reproduce_paper.main`, so this
package must stay importable from the repository root.
"""
